name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  posekit:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: PoseKit tests
        run: swift test --package-path PoseKit
